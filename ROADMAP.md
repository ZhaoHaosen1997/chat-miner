# Chat-Miner 版本路线图

## v1.19 — 底座重构 ✅

> v1.19.0 ~ v1.19.2 已全部完成

- ✅ P0 — 中文分词修复：jieba 精确模式 + 梗百科自定义词典，替代 4-gram 正则
- ✅ P1 — 消息格式化共享层：`services/message_formatter.py` 统一 PII 过滤 + stable_id + 截断
- ✅ P2 — 采样策略重做：金字塔压缩（日报→周报→月报→年报），删除 interestingness 评分
- ✅ P3 — 可配置 Prompt 补全：周报/月报/年报/梗百科全部接入 `get_default_prompt()`
- ✅ P4 — 过滤词生效范围修正：从仅词频统计→消息内容层面也过滤
- ✅ AI 调用日志系统：`services/ai_logger.py` + `PipelineContext` 统一调用入口，前端 AI 日志 tab
- ✅ 提示词格式化 + 高亮规则增强

---

## v1.19.3 — 文档 & 稳定性 🚧

> 文档同步到 v1.19.2 现状 + 高优先级 bug 修复

### P0 — 文档三件套更新
- README.md：版本号→v1.19.2、补 AI 调用日志/7 种提示词/消息格式化层/采样策略、补项目结构缺失文件、补 v1.19 版本历史
- CLAUDE.md：补路由（`/memes`、`/comprehensive/:personaId`、`/event/:eventId`）、补 AI 调用日志/PipelineContext/消息格式化层说明
- newbie-guide.txt：补事件探测/梗百科/鱼塘管理/AI 日志/跨群身份引导

### P1 — start.bat 端口修正
- 8857 → 28856（按 AGENTS.md 本地开发端口规范）

### P2 — DB 连接泄漏修复
- 33 处 `get_conn()`/`close()` 改为 `with db() as conn:` 上下文管理器
- 重点文件：`fish_pond.py`、`passive_events.py`、`weflow_sync.py`、`routers/fish_pond.py`

### P3 — 异常吞没治理
- 40+ 处 `except ... pass` 补充 `logger.warning/error`
- 高风险项优先：`database.py` 迁移代码（776、1397、1443 行）、`fish_pond.py` 画像加载（1340、1522 行）
- 低风险项（预期流程的 fallback）保持 `pass` 但加 `logger.debug`

### P4 — API 错误格式统一
- 统一为 `{"code": N, "message": "...", "data": null}` 格式
- 消除 `HTTPException` 直接抛出，改为 `JSONResponse` 返回
- 前端 `api/index.js` 统一错误处理

---

## v1.19.4 — 安全加固

> 修复安全相关问题和代码质量

### P0 — Pickle 反序列化安全
- `parser.py:490` 改用 JSON 缓存或对 pickle 文件做完整性校验（hash 签名）

### P1 — 上传路径穿越修复
- `groups.py:123-126` 对 `file.filename` 做路径清洗（`Path().name` 取纯文件名，拒绝含路径分隔符的文件名）

### P2 — CORS 配置修正
- `allow_origins=["*"]` + `allow_credentials=True` 改为明确指定开发源（`localhost:5173`、`localhost:28856`）
- 生产模式不启用 CORS（前后端同端口）

### P3 — 内部错误信息脱敏
- `fish_pond.py` 等处 `f"操作失败: {e}"` 改为通用错误消息，详情只写日志

### P4 — `os._exit(0)` 替换
- `log_window.py:360` 改为优雅关闭（`sys.exit(0)`），确保 DB 写入完成

### P5 — 代码清理
- 删除 `config.py` 中 deprecated 的 `EVENT_WINDOW_SIZE/OVERLAP`
- 删除 `call_deepseek_chat()` 旧兼容函数
- 合并 `OLLAMA_HOST` / `LOCAL_LLM_HOST` 重复定义
- 修复 `scheduler.py` logger 重复定义
- `weflow_client.py` 从同步 `requests` 迁移到 `httpx`
- `package.json` 移除未使用的 `axios`
- `asyncio.get_event_loop()` → `asyncio.get_running_loop()`
- 模块级 `asyncio.Lock()` 改为懒初始化

---

## v1.20 — 小白友好

> 降低新用户上手门槛，大版本升级

### P0 — 首次使用引导流程
- 检测到无群组时，显示分步引导卡片（导出→上传→配模型→分析）
- 替代当前首页的简单空状态提示

### P1 — 模型配置预设增强
- 增加国内常用平台预设：硅基流动、零一万物、通义千问、Kimi
- 每个预设自动填充端点 URL + 模型名，用户只需填 API Key

### P2 — 分析结果空状态优化
- 日报/画像生成失败时，解析错误原因并给出具体建议
- 例："API Key 余额不足"→充值链接，"模型超时"→建议换模型或增加超时

### P3 — 一键健康检查
- 设置页增加"诊断"按钮
- 检查项：API 连通性、余额、Ollama 状态、磁盘空间、数据完整性
- 结果以红/黄/绿标识展示

### P4 — 操作反馈增强
- 长时间操作（批量分析、周报生成）显示预估时间
- 基于历史任务平均耗时计算

### P5 — 新手教程内置化
- 将 newbie-guide.txt 内容集成到前端帮助面板
- 右下角 `?` 按钮随时打开，按当前页面显示对应帮助

---

## v1.21 — 报表更有趣

> 让现有分析结果更生动、更有分享欲

### P0 — 日报"群聊热搜榜"
- 类似微博热搜，按话题热度排名
- 🔥 等级标识（1~5 火），配合话题标签
- 仪表盘和日报页均展示

### P1 — 画像"群友对比"
- 选两个群友生成趣味对比卡片
- 维度：谁话多、谁夜猫、谁斗图王、谁话题王
- 适合截图分享

### P2 — 周报/月报"群聊奥斯卡"
- 在现有奖项基础上增加趣味奖项
- 最佳捧哏、话题终结者、表情包之神、深夜电台、早安鸟
- 配合 AI 生成的颁奖词

### P3 — 情绪趋势图
- 日报/周报增加情绪变化折线图（Chart.js）
- 基于每日情绪标签数据绘制
- 直观展示群聊氛围走向

### P4 — 消息类型分析可视化
- 饼图展示文字/图片/表情/链接/视频比例
- 配合趣味标签（"图王群"、"话痨群"、"表情包群"）

### P5 — 年报"时光机"
- 可视化时间轴，展示群聊从创建到现在的关键节点
- 配合动画效果，支持年份切换

---

## v1.22 — 新功能

> 增加全新趣味功能，提升粘性和传播性

### P0 — 群聊"今日运势"
- 每天为每个群友生成趣味运势
- 基于历史发言模式（活跃时段、情绪倾向、话题偏好）
- 轻量级：单次 AI 调用或纯 Python 规则生成
- 仪表盘卡片 + 画像页入口

### P1 — 群聊数据导出/分享
- 将日报/画像/年报导出为精美图片
- Canvas 渲染 → 下载 PNG
- 适合分享到群里，增加传播性

### P2 — "群聊回声"
- 输入一句话，AI 基于群聊风格预测群友们的回复
- 类似"群聊模拟器"但更智能
- 基于画像数据 + 群聊风格 Prompt

### P3 — 群聊"年历"
- 12 个月热力图日历视图
- 每天一格，颜色深浅代表活跃度
- 点击查看对应日报
- 年报页集成

### P4 — AI 问答"群聊百科"
- 基于群聊历史回答问题
- "我们群上个月讨论最多什么？"、"谁最常说 XX？"
- RAG 思路但用现有日报/画像摘要作为上下文
- 设置页需配置专用模型

### P5 — 群聊"成就系统"
- 为群聊整体设定成就
- "连续 7 天有人说话"、"单日 1000 条"、"10 人同时在线"、"累计 10 万条"
- 仪表盘成就墙展示，解锁时弹窗庆祝

---

## v0.x 历史（已完成）

<details>
<summary>点击展开早期版本记录</summary>

### v0.13 — 报告增强 + 数据管理（规划中）
- 报告分享（图片导出）
- 日报/周报/月报对比视图
- 数据导出 + 批量删除

### v0.12 — 模型配置系统 ✅
- 设置页面 CRUD 多模型管理
- 在线模型日报单次 JSON 调用 + 画像单次调用
- 失败自动降级到本地管线

### v0.11 — 年度报告 ✅
- AI 年度主题 + 叙事 + 12 奖项
- 时光轴 + 词云

### v0.10 — 前端全面重设计 ✅
- 群聊杂志风格 + 情绪自适应主题
- 关系网络图（SVG 交互式）

### v0.9 — 群鱼塘 ✅
- 22 种水生生物 + 六维属性
- 掷骰检定 + 每日结算 + 黑市

### v0.8 — QQ 聊天记录导入 ✅
- QQChatExporter V5 格式适配

### v0.7 — 周报/月报 ✅
- DeepSeek 深度推理 + 脱敏架构

### v0.6 — 热搜榜 + 关系图 + 考古 ✅

### v0.1-0.5 — 基础框架 ✅
- 日报/画像 + 多群管理 + 深度画像 + 趣味称号

</details>

---

## 优先级总览

| 版本 | 核心亮点 | 状态 |
|------|----------|------|
| v1.19.2 | 底座重构：格式化统一 + 分词修复 + 采样重做 + AI 日志 | ✅ 完成 |
| v1.19.3 | 文档同步 + DB 连接泄漏 + 异常吞没 + API 错误统一 | 🚧 进行中 |
| v1.19.4 | 安全加固：Pickle/路径穿越/CORS/错误脱敏/代码清理 | 📋 计划中 |
| v1.20 | 小白友好：首次引导/预设增强/健康检查/内置帮助 | 📋 计划中 |
| v1.21 | 报表有趣：热搜榜/群友对比/奥斯卡/情绪图/时光机 | 📋 计划中 |
| v1.22 | 新功能：今日运势/图片导出/群聊回声/年历/AI问答/成就 | 📋 计划中 |
